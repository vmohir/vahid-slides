{
  "name": "@vahid-slides/homepage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p ../../dist && cp -r index.html index.css ../../dist/ && cp ../../slides-style-*.css ../../dist/"
  }
}
